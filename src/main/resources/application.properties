spring.application.name=buddy_english_be
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Driver MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# URL kết nối Azure MySQL (kèm tên database là Buddy_English_DB)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://buddy-db2033.mysql.database.azure.com:3306/Buddy_English_DB?useSSL=true&requireSSL=false&serverTimezone=UTC}

# Thông tin tài khoản Azure
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:buddyadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Xedap2026@}

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

jwt.secret=${JWT_SECRET:QnVkZHlFbmdsaXNoUHJvZHVjdGlvbkF1dGhTZWNyZXRLZXkyMDI2Rm9ySFMzODQ=}
jwt.access-token.expiration-ms=${JWT_ACCESS_TOKEN_EXPIRATION_MS:900000}
jwt.refresh-token.expiration-ms=${JWT_REFRESH_TOKEN_EXPIRATION_MS:604800000}
jwt.issuer=${JWT_ISSUER:buddy-english}

app.cookie.secure=${APP_COOKIE_SECURE:false}
app.cors.allowed-origin=${APP_CORS_ALLOWED_ORIGIN:http://localhost:5173}

# Security & OTP limits
app.otp.expiry-minutes=5
app.security.lockout.max-attempts=5
app.security.lockout.duration-minutes=30

# Mail Server configurations
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:doanchibinh132@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:wjou enbq desa ijnu}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# VNPay payment configurations
vnpay.tmnCode=${VNPAY_TMNCODE:VNPAY_MOCK_TMNCODE}
vnpay.hashSecret=${VNPAY_HASHSECRET:VNPAY_MOCK_SECRET}
vnpay.url=${VNPAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.returnUrl=${VNPAY_RETURN_URL:http://localhost:8080/api/v1/payment/vnpay-callback}
vnpay.version=2.1.0
vnpay.command=pay
vnpay.currCode=VND

# Gemini API configurations
gemini.api.key=${GEMINI_API_KEY:AIzaSyA2XC2lYP-oquXBGmF0fYeeKEv9BjcvKjA}
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/}
gemini.model=${GEMINI_MODEL:gemini-2.5-flash}

# Meshy.ai API configuration
meshy.api.key=${MESHY_API_KEY:msy_dh8NK3fKLXB6ZYTwmZ0BFdVQFI7RxhkcH11q}
meshy.api.url=${MESHY_API_URL:https://api.meshy.ai/}
meshy.frontend.public-path=${MESHY_FRONTEND_PUBLIC_PATH:public/custom_models}
